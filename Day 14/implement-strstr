class Solution(object):
    def strStr(self, haystack, needle):
        
        """
        :type haystack: str
        :type needle: str
        :rtype: int
        """
        if needle not in haystack:
            return -1
        else:
            return haystack.index(needle)
        
thought of using python omw but saw discussion cuz cant code
will try in c++ again

class Solution {
public:
    int strStr(string haystack, string needle) {
        int i=haystack.find(needle);
        if(i>=0)
            return i;
        else
            return -1;
        
    }
};

lmao .find() is the best thing ever lol


there is smn called rabin karp algo, and kpm algo and Z algo to find this in lesser TC
